# GA4 Measurement ID (Admin → Data streams → Web).
# Not a dashboard password, but keep it out of git — use GitHub Actions secrets for deploy.
# Copy to .env.local for local preview: cp .env.example .env.local
VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Public origin used for note canonical URLs and the sitemap.
VITE_SITE_URL=https://new2ords.com
